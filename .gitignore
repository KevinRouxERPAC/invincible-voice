*.egg-info
*.pyc
.DS_Store
services/backend/node_modules/
debug/
# ...but the Android debug-variant resource overlay is source, not a build
# artifact: it makes debug builds permit cleartext for the live-reload dev
# server while release builds forbid it. Keep it tracked.
!/services/frontend/android/app/src/debug/
recordings/
.venv/
# only ignore voices/ in the root directory
/voices/

# env files (can opt-in for committing if needed)
.env*

# Deploiement : secrets et caches a ne jamais committer
API keys.txt
*.deploy-backend.log
.deploy-backend.log
services/frontend/.firebase/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Traefik/HTTPS
certs/

volumes/
.claude/settings.local.json
image.png
model.gguf
iv_screen.png
.vscode/settings.json

# Local test/debug screenshots at the repo root (throwaway artifacts)
/screenshot_test_app_*.png
/ui_*.png

# AI agent files
CLAUDE.md
QWEN.md
.planning/
